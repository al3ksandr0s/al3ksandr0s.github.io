// Definizioni dei tipi (solo commenti per documentazione in JS puro)
// GameMode: 'challenge' | 'challenge-join' | 'duel' | ''
// GameState: { mode, seed, moves, time, matchedPairs, flippedCards, canFlip, timerInterval }
// DuelState: { currentPlayer, player1Moves, player2Moves, seed }
// DecodedChallenge: { seed, moves, time, hash }

export {};
